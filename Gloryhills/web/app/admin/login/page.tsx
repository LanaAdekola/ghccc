import Link from 'next/link';
import {login} from './actions';
import {SubmitButton} from './submit-button';
import {configured} from '@/lib/content';
export default async function Login({searchParams}:{searchParams:Promise<{error?:string;reset?:string}>}){const {error,reset}=await searchParams;return <main id="main" className="page-heading"><p className="eyebrow">CHURCH ADMINISTRATION</p><h1>Welcome back.</h1>{reset==='success'&&<p role="status">Password updated. Sign in with your new password.</p>}{!configured()&&<p className="notice">Sign-in is unavailable: the Supabase project URL and public key are missing. Configure NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY in the server environment, then restart or redeploy the app.</p>}{error&&<p role="alert">{error==='setup'?'Supabase is not configured. Add the project URL and public key, then restart or redeploy.':error==='connection'?'Unable to reach the sign-in service. Check your Supabase configuration and try again.':error==='access'?'This account does not have administrator access. Assign its Auth user ID a role in user_roles.':'Unable to sign in. Check your email, password and email confirmation status.'}</p>}<form className="form" action={login}><label>Email<input name="email" type="email" autoComplete="username" required/></label><label>Password<input name="password" type="password" autoComplete="current-password" required/></label><SubmitButton configured={configured()}/></form><Link href="/admin/forgot-password">Forgot your password?</Link></main>}
