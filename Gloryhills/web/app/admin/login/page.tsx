import {login} from './actions';
import {configured} from '@/lib/content';
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;return <main id="main" className="page-heading"><p className="eyebrow">CHURCH ADMINISTRATION</p><h1>Welcome back.</h1>{!configured()&&<p className="notice">Administrator sign-in will be available after Supabase is configured.</p>}{error&&<p role="alert">{error==='access'?'This account does not have administrator access.':'Unable to sign in. Check your credentials and configuration.'}</p>}<form className="form" action={login}><label>Email<input name="email" type="email" autoComplete="username" required/></label><label>Password<input name="password" type="password" autoComplete="current-password" required/></label><button className="button" disabled={!configured()}>Sign in</button></form></main>}
