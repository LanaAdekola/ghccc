import 'server-only';
export function validateEnvironment(){const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,site=process.env.NEXT_PUBLIC_SITE_URL;if(Boolean(url)!==Boolean(key))throw new Error('Configure both Supabase URL and public key, or leave both unset.');if(url&&!/^https:\/\/[a-zA-Z0-9.-]+\/?$/.test(url))throw new Error('Supabase URL must be the HTTPS project origin only, without /rest/v1 or /auth/v1.');if(site){const parsed=new URL(site);if(!['https:','http:'].includes(parsed.protocol)||parsed.pathname!=='/'||Boolean(parsed.username||parsed.password||parsed.search||parsed.hash))throw new Error('Site URL must be an HTTP(S) origin without a path.');}}
